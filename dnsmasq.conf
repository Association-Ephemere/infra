# Résout photostand.local vers l'IP du PC A sur le réseau local
# Remplace 192.168.x.x par l'IP réelle du PC A
address=/photostand.local/172.20.10.4

# Ne pas utiliser /etc/resolv.conf du container
no-resolv

# Serveurs DNS upstream (pour les autres domaines)
server=8.8.8.8
server=8.8.4.4
